import {ApiResponse, MdMetaColGroup, MdMetaColGroupSpecSearchParam} from "@/packages/types";
import { AxiosInstance } from "axios";

// field group
export const useMdMetaColGroupApi = (apiBase: AxiosInstance) => {
  return {
  };
};
