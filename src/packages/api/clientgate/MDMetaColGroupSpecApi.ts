import {ApiResponse, MdMetaColGroupSpec, MdMetaColGroupSpecSearchParam} from "@/packages/types";
import { AxiosInstance } from "axios";

export const useMdMetaColGroupSpecApi = (apiBase: AxiosInstance) => {
  return {
  };
};
